env: staging

# Service-specific overrides

product:
  image:
    tag: "product-service-v1.0.2"
  replicaCount: 2
  rollout:
    enabled: true
    strategy: canary

inventory:
  image:
    tag: "inventory-service-v1.0.1"
  replicaCount: 2
  rollout:
    enabled: true
    strategy: canary

order:
  image:
    tag: "order-service-v1.0.1"
  replicaCount: 2
  rollout:
    enabled: true
    strategy: canary

payment:
  image:
    tag: "payment-service-v1.0.1"
  replicaCount: 2
  rollout:
    enabled: true
    strategy: canary

noti:
  image:
    tag: "notification-service-v1.0.1"
  replicaCount: 1
  rollout:
    enabled: true
    strategy: canary

client:
  image:
    tag: "client-v1.0.1"
  replicaCount: 2
  rollout:
    enabled: true
    strategy: canary